# The core pipeline uses only packages already present in the Kaggle base image
# and in any standard scientific Python install. Versions are lower bounds, not pins:
# the pipeline is tested on the versions Kaggle currently ships.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3      # sklearn.cluster.HDBSCAN is used by the optional branch
matplotlib>=3.7
openpyxl>=3.1          # reading the .xlsx export
joblib>=1.3            # model persistence

# Optional - only for the multilingual embedding cross-check in section 7.
# Needs Kaggle "Internet: On"; a GPU makes it fast but is not required.
# sentence-transformers>=2.2
